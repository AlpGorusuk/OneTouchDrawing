using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RetryState : State
{
    public RetryState(GameplayManager gameManager, StateMachine stateMachine) : base(gameManager, stateMachine)
    {
    }
    
}
